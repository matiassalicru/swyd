# Project Conventions

## Code Style

- Use functional approach with arrow functions: `const myFunction = () => {}` instead of `function myFunction() {}`.
- This applies to all functions: components, hooks, helpers, handlers, and utilities.
